# 다음에 할 일

> 마지막 갱신: 2026-09-21

DB 마이그레이션(0015·0016·seed)과 코드 배포는 **끝났다.** 질문 83개가 들어가 있고
커밋도 전부 `origin/main`에 올라가 있다.

---

## 배포 후 확인할 것

앱에서 직접 봐야 하는 것들. 아직 안 봤으면 여기부터.

### 카드 짝맞추기 — 어려움에서 화면이 잘리는지

가장 급한 확인이다. 미리보기 중에 24장이 **스크롤 없이 전부 보여야** 한다.
아래 줄이 잘리면 "모두에게 같은 정보를 준다"는 전제가 깨져서 제한을 거는 근거가 없어진다.

카드를 정사각형으로 바꿔서 390px 폰에서는 525px(가용 584px)로 들어오지만,
아이폰 SE 같은 작은 화면은 여전히 빡빡할 수 있다.

**잘리면** `lib/games/config.ts`의 `DIFF[*].n.mem`을 `8/10/12 → 6/8/10`으로 내린다.
최대 4×5가 되어 거의 모든 폰에 들어온다. 난이도는 이미 미리보기 시간으로 조절하므로
쌍이 줄어도 단계 차이는 유지된다.

### 퀴즈 출제 이력이 쌓이는지

상식 퀴즈를 **두 판** 돌린 뒤:

```sql
select qid, seen_count,
       to_char(seen_at at time zone 'Asia/Seoul', 'MM-DD HH24:MI') as 본시각
  from public.quiz_seen order by seen_at desc limit 30;
```

24행 정도면 정상. **0행이면 이력이 안 쌓이는 것**이고, 그래도 게임은 무작위 출제로
돌아가므로 화면으로는 구분이 안 된다 — 그래서 이 확인이 필요하다.

> SQL Editor에서 `mark_quiz_seen`을 직접 호출해봐도 확인이 안 된다.
> `auth.uid()`가 null이라 0행이 들어가고 조용히 넘어간다.

### 가족 회상 질문

```
어머니 계정  →  홈에 분홍/주황 "오늘의 질문" 카드
             →  누르면 /recall, 답 쓰고 보내기
형진 계정    →  /connect → "추억 이야기" (답장 대기 있으면 주황 + 개수)
```

---

## 손봐야 할 것

### 게임 난이도 숫자 조정 (해보고 나서)

`lib/games/config.ts`의 `MEM` 값은 추정치다. 어머니가 한 판 해보고 감이 다르면
이 숫자만 바꾸면 된다.

```ts
previewMsPerPair: { easy: 750, normal: 500, hard: 333 }  // 미리보기가 짧으면 ↑
moveLimitPerPair: 4.5                                     // 빡세면 5.5~6
```

### 색깔 맞추기 — 아직 손 안 댄 게임

6종 중 유일하게 검토가 안 됐다. 확인된 문제:

- **난이도 차등이 없다.** `stroopRound()`가 난이도를 받지 않아 쉬움/보통/어려움이
  같은 문제를 낸다. 다른 건 문항 수(12/16/20)와 배수뿐 — 어려움이 "어려운" 게
  아니라 "긴" 것이다
- **시간 제한이 없다.** 스트룹은 본래 속도 과제다. 간섭 효과(글자를 읽는 자동 처리가
  색 명명을 방해)는 빠르게 반응할 때만 나타난다. 천천히 "글자는 빨강인데 색은
  파랑이네" 하고 추론하면 100% 맞고, 훈련 효과의 핵심이 빠진다
- 보기가 5개인데 2열 그리드라 마지막 줄에 하나만 남는다
- 답할 때마다 "다음 문제" 버튼을 눌러야 해서 리듬이 끊긴다

### 단어 맞추기 은행 추가 확충

45건이면 같은 날 중복은 없지만 2일차부터 일부 겹친다.
양방향 파생("실 — 무엇과 짝일까요?")도 가능하지만, **연상 게임은 오답을 무작위로
뽑으면 정답이 두 개가 되므로** 손으로 골라야 한다("연필"에 "종이" 같은 경우).

### 가족 회상 질문 문구 손보기

seed 83개 중 집안 상황에 맞게 고칠 것:

- `아버지`가 들어간 문항들 — 호칭·상황이 다르면 수정
- 9번 `어머니가 해주시던 음식` — 외할머니를 뜻한 것. 집에서 쓰는 호칭으로

문구 수정은 DB에서 직접 한다. `prompt`가 unique 키라서 seed 파일을 고치면
새 질문으로 들어간다.

```sql
update public.family_questions set prompt = '...' where id = '...';
```

빼려면 지우지 말고 `active = false` — 답변 기록이 살아 있어야 한다.

### 민감한 질문 3개

`supabase/seed/family_questions.sql` 맨 아래 주석으로 분리해 뒀다.
돌아가신 분 이야기가 나올 수 있어서 **곁에 있을 때만** 켤 것.

```
· 아버지랑 제일 크게 다퉜던 일, 기억나세요?
· 형진이 키우면서 제일 힘들었던 때는 언제예요?
· 요즘 제일 보고 싶은 사람이 누구예요?
```

### Vercel 환경변수 정리 (선택)

`NEXT_PUBLIC_SUPABASE_URL`이 Secret 타입으로 저장돼 있어 값을 볼 수 없다.
`NEXT_PUBLIC_`은 브라우저에 노출되는 값이라 Secret으로 둘 의미가 없고,
이것 때문에 프로젝트 ref를 찾는 데 시간을 썼다. Config로 바꾸면 나중에 확인이 쉽다.
(Vercel은 Secret → Config 전환을 막으므로 삭제 후 재등록해야 한다.)

---

## 하기로 안 한 것

- **푸시 알림** — 답장이 오면 어머니에게 알림. `web-push` 인프라는 이미 있다
  (`/api/cron/notify`)
- **음성 답변** — `family_answers`에 `audio_path` 컬럼만 추가하면 되게 스키마를
  잡아뒀다. 글로 쓰면 한 줄, 말로 하면 이야기가 나오므로 언젠가는 할 만하다

---

## 다음 후보

### 상식 퀴즈 은행 확충 (LLM 배치 사전 생성 + 사람 검수)

서버 출제 구조가 있어 번들 걱정 없이 키울 수 있다. 500문항대로 올리면
무중복 기간이 7일 → 3주.

**런타임 LLM 생성은 반대다** — 고령자에게 틀린 사실을 정답으로 내면 신뢰가 무너진다.
오프라인 생성 → 검수 → 커밋이 맞다.

### 비어 있는 인지 영역

6종이 다루는 것과 빠진 것:

```
상식 퀴즈    의미기억        카드 짝맞추기  시각 작업기억
단어 맞추기   언어·연상       순서 기억      순차 작업기억
숫자 계산    계산·주의       색깔 맞추기    억제·간섭통제
가족 질문    일화·자전적기억
```

- **처리 속도** — 시간 압박이 있는 게임이 하나도 없다. 인지 노화에서 가장 먼저
  떨어지는 지표인데 측정이 안 된다. 색깔 맞추기에 시간 제한을 넣으면 이 축이
  부분적으로 채워진다
- **공간 지각** — 완전히 없다. 간단한 미로나 순서 배열 같은 것

---

## 참고

### 이번 작업에서 새로 생긴 파일

```
lib/games/quiz-bank.ts                        문제 은행 + 출제 로직 (server-only)
lib/games/serve.ts                            이력 조회 → 출제 → 기록
lib/recall/queries.ts                         오늘의 질문 선택 · 자녀 피드
app/(app)/recall/                             어머니 회상 화면
app/connect/[seniorId]/recall/                자녀 답장 화면
supabase/migrations/0015_quiz_seen.sql
supabase/migrations/0016_family_questions.sql
supabase/seed/family_questions.sql
```

### 알아두면 좋은 것

- **문제 은행은 `server-only`다.** 클라이언트에서 import 하면 빌드가 깨진다.
  의도한 것 — 은행 전체가 클라 번들에 실리면 은행을 키울 수 없다
- **`quiz_seen`의 식별자는 두 개다.** `source`는 한 판 안에서 같은 지식이 두 번
  나오는 것을 막고, `qid`는 사용자별 이력 키로 방향(fwd/rev)까지 구분한다.
  `qid`를 바꾸면 그 문항의 이력이 리셋되니 한번 정하면 바꾸지 말 것
- **가족 회상 질문은 채점하지 않는다.** 정답 컬럼·점수·포인트가 없다.
  목적이 "맞히기"가 아니라 "생각하게 하기"다
- **`0011_id_login.sql`은 돌리지 말 것.** 기존 계정의 이메일 도메인을 바꾸는
  파일이라 지금 돌리면 로그인이 깨질 수 있다
- 레포 전체 lint 에러는 현재 **0**이다 (`sequence-game.tsx` 2건 해소)
